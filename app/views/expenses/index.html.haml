%h1 Административные расходы

%br

.row.main-content.col-sm-offset-2.col-sm-8
  .table-responsive
    = form_tag :expenses_update, {:method => :post} do
      %table.table.table-hover
        %thead
          %tr
            %th.col-sm-9 Наименование
            %th.col-sm-3
              %button.btn.btn-sm.btn-success Сохранить
        %tbody
          - @expenses.each do |exp|
            %tr
              %td= exp.name
              %td
                .form-group
                  .col-sm-10
                    %input.form-control.col-sm{ type: 'number', max: '100', min: '0', step: '0.01', name: "values[#{exp.id}]", value: exp.percent.to_s.sub(/\.0+$/,''), required: 'required' }
          %tr
            %td
              %b Итого
            %td.text-center
              %b= "#{Expense.total_sum.to_s.sub(/\.0+$/,'')} %"
